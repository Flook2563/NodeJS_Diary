<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Diary</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
    <%- include('../layout') %>
    <div class="py-2">
        <a href="/diary/add" class="btn btn-primary">เขียนไดอารี่</a>
    </div>
    
    <hr>
    <h3><%= data %></h3>
    <hr>
    <% Object.values(diarys).forEach(function(diary){%>
        <p><b>หัวข้อไดอารี่ : </b><%= diary.title %></p>
        <p><b>รายละเอียด : </b> <%= diary.detail %></p>
        <p><b>ผู้เขียน: </b> <%= diary.author %></p>
        <a href="/diary/edit/<%= diary._id %>">แก้ไข</a>
        <a href="/diary/delect/<%= diary._id %>" onclick="return confirm('ต้องการลบหรือไม่ ?')">ลบ</a>
        <hr>
    <% }) %>
</body>
</html>